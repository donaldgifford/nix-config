# Tokyonight Dark — per-shell theme env (sourced from shell.nix initContent)
# BAT_THEME matches the default in config/bat/config; fzf colors come from
# programs.fzf defaultOptions, so nothing to append here.
export BAT_THEME="Enki-Tokyo-Night"
export EZA_CONFIG_DIR="$HOME/.config/themes/current/eza"
